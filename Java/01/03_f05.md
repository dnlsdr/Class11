Írasd ki a számsort 1-től n-ig `do-while` ciklus segítségével!

**Lépések:**

1. Deklarálj egy `int` típusú `n` változót, valamint egy `int` típusú `i` változót 1 kezdőértékkel inicializálva!
2. Rendelj egy értéket az `n` változóhoz!
3. Használj `do-while` ciklust a számok sorozatának kiíratására 1-től n-ig!
