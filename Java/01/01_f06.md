Adj meg egy szöveges értéket (String), és írasd ki a hosszát!

**Lépések:**

1. Hozz létre egy új osztályt ehhez a feladathoz, és adj hozzá egy `main` metódust!
2. Deklarálj egy `text` nevű `String` változót!
3. Rendeld a "Java Programming" értéket a `text` változóhoz!
4. Kérdezd le a `text` hosszát, majd tárold el ezt egy változóban! Adj egy értelmes nevet a változónak. A típusa `int` legyen, mivel a hossz nem lebegőpontos szám.
5. Írasd ki a hosszt!

